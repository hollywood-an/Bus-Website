{
  "name": "bus-website",
  "private": true,
  "description": "OSU bus tracker — Vite client (bus-capacity-tracker/) + Hono agent proxy (server/).",
  "scripts": {
    "dev": "concurrently -n client,server -c blue,green \"npm --prefix bus-capacity-tracker run dev\" \"npm --prefix server run dev\"",
    "dev:client": "npm --prefix bus-capacity-tracker run dev",
    "dev:server": "npm --prefix server run dev",
    "install:all": "npm install && npm --prefix bus-capacity-tracker install && npm --prefix server install"
  },
  "devDependencies": {
    "concurrently": "^9.1.0"
  }
}
