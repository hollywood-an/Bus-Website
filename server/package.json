{
  "name": "bus-agent-server",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Hono proxy that runs the OSU bus agent loop server-side (holds the Anthropic key).",
  "scripts": {
    "dev": "tsx watch src/index.ts",
    "start": "tsx src/index.ts",
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.100.1",
    "@hono/node-server": "^2.0.4",
    "dotenv": "^17.4.2",
    "hono": "^4.12.23"
  },
  "devDependencies": {
    "@types/node": "^25.9.1",
    "tsx": "^4.22.4",
    "typescript": "^6.0.3",
    "vitest": "^4.1.7"
  }
}
